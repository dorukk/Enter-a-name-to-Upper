static void Main(string[] args)
        {
            general("Doruk", 10);
            
            Console.ReadKey();

        }
        static void general(string name, int time)
        {
            for (int i = 0; i < time; i++)
            {
                Console.WriteLine(name);
            }
